#include "main.h"
/**
 *_is lower  -returns 1 if lowercase alphabet
 *and 0 other wis
 *Return : 0 for uppercase, 1 for lowercas
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return(1)
	}

	els
	{
		retrun (0)
	}
}

